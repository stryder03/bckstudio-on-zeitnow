import {container} from "../../nextjs-material-kit";

const mainLogoStyle = {
    container: {
        zIndex: "12",
        color: "#FFFFFF",
        textAlign: "center",
        ...container
    },
};

export default mainLogoStyle;
